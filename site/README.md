# site/

Static catalog UI, published via GitHub Pages. See
[`../docs/SITE-SPEC.md`](../docs/SITE-SPEC.md).

Currently a placeholder — `index.html` links to the generated
`public/catalog.json` (written by `../scripts/build-index.mjs`) until the
real catalog UI is built.
