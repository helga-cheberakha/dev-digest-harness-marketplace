{
  "name": "dev-digest-catalog-site",
  "private": true,
  "type": "module",
  "scripts": {
    "build:index": "node ../scripts/build-index.mjs",
    "predev": "npm run build:index",
    "prebuild": "npm run build:index",
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "dompurify": "^3.2.0",
    "marked": "^15.0.0",
    "react": "^19.1.0",
    "react-dom": "^19.1.0"
  },
  "devDependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "vite": "^6.0.0"
  }
}
